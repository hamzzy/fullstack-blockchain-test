query InspectationDetailsOnly($car: String!) {
  inspectationDetailsEntities(
 where: {car_id_contains: $car}    ) {
    brake
    bumpers
   car_id {
      id
    }   
    engine
    interior
    id
    light
    milleage
    tyres
  }
}
